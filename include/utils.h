#ifndef UTILS_H
# define UTILS_H

# include <limits.h>
# include <stdlib.h>
# include <stdbool.h>

int		ft_atoi(const char *str, bool *ok);
long	ft_atol(const char *str, bool *ok);
int		ft_isdigit(int c);

#endif
